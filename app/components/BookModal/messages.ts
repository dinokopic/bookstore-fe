/*
 * BookModal Messages
 *
 * This contains all the text for the BookModal component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.BookModal";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BookModal component!",
  },
});
