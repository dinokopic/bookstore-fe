/*
 * BookPagination Messages
 *
 * This contains all the text for the BookPagination component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BookPagination';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookPagination component!',
  },
});
