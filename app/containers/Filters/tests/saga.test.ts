/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import filtersSaga from '../saga';

// const generator = filtersSaga();

describe('filtersSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
