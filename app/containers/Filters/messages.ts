/*
 * Filters Messages
 *
 * This contains all the text for the Filters container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Filters';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Filters container!',
  },
});
